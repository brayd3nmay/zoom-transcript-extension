{
  "name": "super-zoom",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "gen-icons": "node scripts/gen-icons.js",
    "build:content": "esbuild content/main.js --bundle --format=iife --outfile=content.js",
    "test": "node --experimental-test-module-mocks --test 'tests/**/*.test.js' 'tests/**/*.test.mjs'"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "jsdom": "^29.1.1"
  }
}
